import { BancoNomePipe } from './banco-nome.pipe';

describe('BancoNomePipe', () => {
  it('create an instance', () => {
    const pipe = new BancoNomePipe();
    expect(pipe).toBeTruthy();
  });
});
